diff --git a/src/adder.test.ts b/src/adder.test.ts
new file mode 100644
index 0000000..5046291
--- /dev/null
+++ b/src/adder.test.ts
@@ -0,0 +1,8 @@
+import { Adder } from "./adder.js";
+
+const adder = new Adder();
+console.assert(adder.add(2, 3) === 5, "2 + 3 should be 5");
+console.assert(adder.add(-1, 1) === 0, "-1 + 1 should be 0");
+console.assert(adder.add(0, 0) === 0, "0 + 0 should be 0");
+
+console.log("Adder tests passed.");
diff --git a/src/multiplier.test.ts b/src/multiplier.test.ts
new file mode 100644
index 0000000..0cb3cf5
--- /dev/null
+++ b/src/multiplier.test.ts
@@ -0,0 +1,8 @@
+import { Multiplier } from "./multiplier.js";
+
+const multiplier = new Multiplier();
+console.assert(multiplier.multiply(2, 3) === 6, "2 * 3 should be 6");
+console.assert(multiplier.multiply(-1, 1) === -1, "-1 * 1 should be -1");
+console.assert(multiplier.multiply(0, 5) === 0, "0 * 5 should be 0");
+
+console.log("Multiplier tests passed.");
diff --git a/src/subtractor.test.ts b/src/subtractor.test.ts
new file mode 100644
index 0000000..19ab6bb
--- /dev/null
+++ b/src/subtractor.test.ts
@@ -0,0 +1,14 @@
+import { Adder } from "./adder.js";
+import { Multiplier } from "./multiplier.js";
+import { Subtractor } from "./subtractor.js";
+
+const adder = new Adder();
+console.assert(adder.add(2, 3) === 5, "2 + 3 should be 5");
+
+const multiplier = new Multiplier();
+console.assert(multiplier.multiply(2, 3) === 6, "2 * 3 should be 6");
+
+const subtractor = new Subtractor();
+console.assert(subtractor.subtract(5, 3) === 2, "5 - 3 should be 2");
+
+console.log("All assertions passed.");
diff --git a/src/subtractor.ts b/src/subtractor.ts
new file mode 100644
index 0000000..1c7db49
--- /dev/null
+++ b/src/subtractor.ts
@@ -0,0 +1,5 @@
+export class Subtractor {
+  subtract(a: number, b: number): number {
+    return a - b;
+  }
+}
